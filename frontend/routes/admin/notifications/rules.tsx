import { define } from "@/utils.ts";
import NotificationRules from "@/islands/NotificationRules.tsx";

export default define.page(function NotificationRulesPage() {
  return <NotificationRules />;
});
