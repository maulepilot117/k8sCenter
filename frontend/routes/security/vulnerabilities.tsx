import { define } from "@/utils.ts";
import VulnerabilityDashboard from "@/islands/VulnerabilityDashboard.tsx";

export default define.page(function VulnerabilitiesPage() {
  return <VulnerabilityDashboard />;
});
