import { define } from "@/utils.ts";
import ESOClusterStoresList from "@/islands/ESOClusterStoresList.tsx";

export default define.page(function ClusterStoresPage() {
  return <ESOClusterStoresList />;
});
