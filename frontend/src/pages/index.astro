---
// Placeholder only. U5 needs *something* at "/" to prove the dispatch chain
// end to end (GET / returns a success status, and exercises the Astro
// fallthrough after the headers/ws/static branches). U7 replaces this with
// the real application shell and layout -- no real page content belongs
// here.
---

<p>k8sCenter — placeholder page (replaced by U7).</p>
